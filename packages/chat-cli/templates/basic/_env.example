# Frontend chat endpoint
# Point this to your own server proxy instead of a model vendor API URL.
VITE_CHAT_API_ENDPOINT=/api/chat

# Optional:
# If your backend runs on another origin, point the frontend to that address.
# VITE_CHAT_API_ENDPOINT=http://localhost:3000/api/chat
