# Chat Refactor Experience Log

This file is the lightweight tracker for recurring refactor lessons.

Use it when a lesson:

- repeats across slices
- blocks or reshapes review
- causes multiple rewrites
- clearly changes how later implementation should proceed

Do not use it for every finished task.

## Status Meanings

- `candidate`:
  the lesson is recurring, but not yet stable enough to prescribe broadly
- `promoted`:
  the lesson is stable enough to appear in `PLAYBOOK.md`
- `dropped`:
  the lesson was tracked, but should not become standing guidance

## Entries

| ID | Status | Lesson | Why it matters | Evidence |
| --- | --- | --- | --- | --- |
| K-001 | `promoted` | new behavior should move into owning runtime modules before scaffold or bridge layers are widened | prevents refactor work from re-creating config projection and keeps slices reviewable | `2026-04-21-phase-1a-root-bootstrap.md`, `2026-04-21-phase-1b-page-baseline.md`, `PLAYBOOK.md#1-move-new-behavior-toward-runtime-ownership-not-scaffold-relay` |
| K-002 | `promoted` | message targeting should use `messageId / messageIds`, not new `messageIndex` semantics | avoids edit/retry/regenerate/custom-action drift when message order changes | `2026-04-21-phase-1a-root-bootstrap.md`, `PLAYBOOK.md#2-prefer-stable-ids-over-positional-semantics` |
| K-003 | `promoted` | public composition surfaces need one clear owner and narrow compatibility delegates | avoids ambiguous ownership when `Page` or other official surfaces are introduced | `2026-04-21-phase-1b-page-baseline.md`, `PLAYBOOK.md#3-keep-a-single-owner-for-each-public-composition-surface` |
| K-004 | `promoted` | bridge subsets should only expand when code, tests, and docs all agree | keeps `createRuntimeFromConfig(config)` additive instead of aspirational | `config-bridge-matrix.md`, `2026-04-21-phase-1b-page-baseline.md`, `PLAYBOOK.md#4-expand-bridge-subsets-only-when-they-are-implemented-and-evidenced` |
| K-005 | `candidate` | mounted `Root + Page` proof should be added soon after a baseline slice lands, not deferred too long | source/runtime proof can still leave ownership drift or composition regressions undetected | `2026-04-21-phase-1b-mounted-root-page-integration.md`, `2026-04-21-phase-1b-page-baseline.md`, `alignment-tracker.md` |
| K-006 | `candidate` | compatibility delegates should be contract-tested whenever ownership shifts | without explicit tests, wrapper layers quietly grow back into second owners | `public-surface.test.mjs`, `workspace-slot-contract.test.mjs` |
| K-007 | `candidate` | once a composition surface becomes official, give it a narrow input boundary instead of letting it read broad preset or scaffold buckets directly | it keeps ownership explicit and makes later relay tightening additive instead of a page rewrite | `2026-04-21-phase-1b-page-relay-tightening.md`, `public-surface.test.mjs`, `page-region-contract.md` |
| K-008 | `candidate` | after an official composition surface gets a narrow input boundary, the nearest default primitives should consume those explicit inputs before falling back to compatibility relay | otherwise ownership stays nominally correct while defaults still leak back through scaffold lookups one component lower | `2026-04-21-phase-1b-default-page-primitive-tightening.md`, `public-surface.test.mjs`, `api-runtime.md` |
| K-009 | `promoted` | workspace owner chains need the same explicit-input treatment as page owner chains once the default page path is stabilized | otherwise sidebar and mobile-sheet paths keep reintroducing display-default relay through scaffold lookups even after page ownership is narrowed; Phase 3B mobile parity only became real once the default workspace layout fell back from page `shell` input to runtime-owned workspace shell state instead of raw scaffold presets | `2026-04-21-phase-1b-workspace-relay-tightening.md`, `2026-04-22-phase-3b-workspace-mobile-parity-baseline.md`, `workspace-slot-contract.test.mjs`, `page-region-contract.md`, `PLAYBOOK.md#12-keep-workspace-mobile-fallback-on-explicit-shell-input-or-runtime-workspace-state` |
| K-010 | `promoted` | once explicit owner inputs reach a primitive, those inputs should become authoritative and compatibility relay should become explicit instead of staying silently active | otherwise ownership looks explicit on the surface, but the old relay still decides the effective renderer or display behavior one layer lower | `2026-04-21-phase-1b-layout-renderer-relay-pruning.md`, `2026-04-21-phase-1b-header-history-relay-pruning.md`, `2026-04-21-phase-1b-welcome-message-list-relay-pruning.md`, `PLAYBOOK.md#6-once-owner-inputs-are-explicit-compatibility-relay-must-become-explicit` |
| K-011 | `promoted` | phase review packets should carry an explicit evidence index and current drift summary instead of only narrative status text | otherwise review meetings drift back into live repository archaeology and the pass/blocked decision becomes harder to anchor | `review-b-phase-1a-report-and-phase-1b-kickoff/REVIEW_B_SPEC_DETAIL.md`, `review-c-phase-1b-report-and-phase-2-kickoff/REVIEW_C_SPEC_DETAIL.md`, `PLAYBOOK.md#7-phase-reviews-should-package-evidence-and-drift-explicitly` |
| K-012 | `candidate` | when a blackbox cutover must coexist with old config shapes, land the fully target-shaped entry path first and keep legacy fallback explicit instead of mixing both contracts in one hidden branch | it lets a new phase start proving the target path without pretending the old compatibility surface has already been retired | `2026-04-21-phase-2-blackbox-root-page-kickoff.md`, `2026-04-21-phase-2-blackbox-trchat-cutover-baseline.md`, `src/components/core/Chat.vue` |
| K-013 | `candidate` | compatibility callbacks should be classified by target owner domain instead of treated as an all-or-nothing blackbox gate | it let `onFinish / onError` move into `lifecycle.afterReceive / error` without prematurely dragging `onBeforeSend / onMessageAction / onModelChange` into the new entry path | `2026-04-21-phase-2-blackbox-lifecycle-callback-expansion.md`, `src/runtime/config/blackboxEntry.ts`, `generated/config-bridge-matrix.md` |
| K-014 | `candidate` | when a legacy wrapper is only the serialized form of the already-frozen target contract, normalize it at the entry boundary instead of routing it through legacy projection | it widened the supported blackbox surface without implying that old `ChatConfig` semantics were already safe to promote | `2026-04-21-phase-2-serialized-target-config-classification.md`, `src/runtime/config/blackboxEntry.ts`, `tests/integration/trchat-blackbox-root-page.test.mjs` |
| K-015 | `promoted` | only promote a legacy config subset when every admitted field collapses cleanly into already-frozen target owner domains at the entry boundary | `Phase 2` only stayed reviewable because old `ChatConfig` first promoted `models / providers / defaults`, then separately promoted `appearance / ui.brand / ui.welcome`, then promoted only `layout.contentLayout`, and then only `shell.variant / shell.leftRegion / shell.rightRegion`, instead of reopening broad scaffold projection | `2026-04-21-phase-2-old-chat-config-request-subset-classification.md`, `2026-04-21-phase-2-old-chat-config-display-default-classification.md`, `2026-04-21-phase-2-old-chat-config-content-layout-classification.md`, `2026-04-21-phase-2-old-chat-config-shell-subset-classification.md`, `src/runtime/config/blackboxEntry.ts`, `PLAYBOOK.md#8-promote-legacy-config-only-in-owner-aligned-slices` |
| K-016 | `promoted` | when a legacy field only lands in preset projection and has no frozen target owner path, prove explicit fallback instead of inventing a pseudo-normalization | `ui.prompts`, then `layout.variant / placements`, and now `shell.viewState` all looked adjacent to admitted blackbox subsets, but keeping them on scaffold fallback avoided smuggling preset/welcome, role-placement, or old shell display-state semantics back into the target entry path | `2026-04-21-phase-2-old-chat-config-prompts-classification.md`, `2026-04-21-phase-2-old-chat-config-layout-variant-placements-classification.md`, `2026-04-21-phase-2-old-chat-config-shell-view-state-classification.md`, `tests/runtime/blackbox-entry.test.mjs`, `tests/integration/trchat-blackbox-root-page.test.mjs`, `PLAYBOOK.md#9-when-a-legacy-field-has-no-frozen-target-owner-prove-fallback-instead-of-inventing-a-bridge` |
| K-017 | `promoted` | once sender and attachments already have frozen runtime owners, move the default owner path onto those runtime defaults and handoff points before keeping compatibility feature presets alive as fallback | `Phase 3A` sender parity only became real once `voice / wordCount` stopped depending on `senderActionsFeature` in the default owner path and started reading `sender runtime.defaults` first, and sender-plus-attachments parity only became real once `ChatSender` plus `ChatAttachments` could read runtime-owned upload config, list config, and pending attachments without attachment feature context | `2026-04-22-phase-3a-sender-runtime-voice-wordcount-parity.md`, `2026-04-22-phase-3a-sender-attachments-parity-hardening.md`, `src/components/core/ChatSender.vue`, `src/components/attachments/ChatAttachments.vue`, `tests/integration/root-page-mounted.test.mjs`, `PLAYBOOK.md#11-keep-sender-and-attachments-ui-on-runtime-owned-defaults-and-handoff-once-they-exist` |
| K-018 | `promoted` | once a runtime owner already exposes message-extension config, the nearest extension UI should fall back to that runtime source before depending on higher-level prop relay | Phase 3A action parity only became durable once `ChatFeedback` could read `runtime.message.getActions()` and runtime `actionMode`, feedback parity only became real once `ChatFeedback` plus the default body region could fall back to runtime-owned feedback enablement, and renderer parity only became real once `ChatLayout` could read `runtime.message.config.renderers` directly instead of assuming page-input or scaffold projection had already happened | `2026-04-22-phase-3a-message-runtime-action-fallback.md`, `2026-04-22-phase-3a-feedback-runtime-enablement-and-transform-proof.md`, `2026-04-22-phase-3a-renderer-runtime-owner-fallback.md`, `src/components/feedback/useChatFeedback.ts`, `src/components/feedback/ChatFeedback.vue`, `src/components/core/default-renderer/ChatDefaultBodyRegion.vue`, `src/components/core/ChatLayout.vue`, `tests/runtime/message-actions.test.mjs`, `tests/integration/root-page-mounted.test.mjs`, `PLAYBOOK.md#10-let-nearest-extension-ui-fall-back-to-runtime-owned-message-extension-config` |
| K-019 | `promoted` | when a parity slice is about ownership, add a mounted proof at the nearest owner region or primitive instead of relying only on full-page rendering | Phase 3A and Phase 3B slices repeatedly became clearer once the proof mounted `ChatLayout`, then `ChatSender + ChatAttachments`, then `ChatWorkspaceLayout`, and finally `ChatDefaultHeaderRegion + ChatDefaultFooterRegion`; full-page rendering alone was too coarse and could hide relay/context leakage | `2026-04-22-phase-3a-renderer-runtime-owner-fallback.md`, `2026-04-22-phase-3a-sender-attachments-parity-hardening.md`, `2026-04-22-phase-3b-workspace-mobile-parity-baseline.md`, `2026-04-22-phase-3b-history-model-mcp-parity-baseline.md`, `tests/integration/root-page-mounted.test.mjs`, `PLAYBOOK.md#12-prove-parity-at-the-nearest-owner-region-or-primitive-not-only-through-the-full-page` |
| K-020 | `candidate` | when a downstream package fails type-check on symbols that already exist in a sibling package source tree, verify the sibling export index and rebuilt dist artifacts before patching the downstream consumer | the Phase 4 hardening blocker was not in `packages/chat`; `ChatHeader`, workspace sidebar components, and `shared/utils/iconMap.ts` only recovered once the missing icon exports were added to `packages/svgs/src/components/index.ts` and the sibling package was rebuilt | `packages/svgs/src/components/index.ts`, `packages/svgs/dist/index.d.ts`, `packages/chat/src/components/core/ChatHeader.vue`, `packages/chat/src/shared/utils/iconMap.ts`, `2026-04-22-phase-4-hardening-review-d-kickoff.md` |
| K-021 | `candidate` | once the official package entry ladder is frozen, update the package README and official demos in the same slice instead of leaving helper-heavy compatibility examples as the default story | Phase 4 closure only became trustworthy once the demo app and package-level guidance all taught the same `TrChat -> Root + Page -> Root + primitives` ladder, rather than keeping `Scaffold`, `useChatKit`, internal demo helpers, and compatibility MCP wiring as the first examples people see | `packages/chat/README.md`, `packages/chat/demo/src/components/BlackboxDemo.vue`, `packages/chat/demo/src/components/WhiteboxDemo.vue`, `packages/chat/demo/src/components/GranularWorkspaceDemo.vue`, `2026-04-22-phase-4-official-demo-helper-closure.md` |
| K-022 | `candidate` | before deleting compatibility code, freeze one official-path test boundary and split the fast package-local gate from the slower e2e migration set | post-closure cleanup only becomes safely repeatable once every slice can reuse the same `packages/chat/tests` hard gate and the same keep/adapt/retire classification for `packages/test/src/chat`, instead of renegotiating test scope every time code is pruned | `packages/chat/docs/refactor/process/test-boundary-baseline.md`, `packages/test/src/chat/README.md`, `2026-04-23-post-closure-test-boundary-baseline.md` |
| K-023 | `candidate` | promote retained e2e by adapting existing scenes toward the official entry ladder before deleting or rewriting the rest of the suite | post-closure cleanup moved faster once the first Playwright batch reused the existing folder but rewired `blackbox`, `whitebox`, and `granular` scenes around target `TrChatConfig`, `Root + Page`, and `Root + primitives`, instead of trying to replace the whole e2e app first | `packages/test/src/chat/scenarios/officialSceneConfig.ts`, `packages/test/src/chat/index.spec.ts`, `packages/test/src/chat/history.spec.ts`, `packages/test/src/chat/request-lifecycle.spec.ts`, `packages/test/src/chat/scenario-specs/renderer-registry.spec.ts` |
| K-024 | `candidate` | when a field is already admitted in the frozen official contract, prefer repairing the missing owner projection before retiring the test that exposed it | the `welcome-prompts` handoff only became coherent once `ui.welcome.prompts` was actually projected through the root bootstrap path into `Root + Page`; reclassifying that spec as legacy-only would have hidden a real contract-to-implementation gap instead of closing it | `packages/chat/src/root/createRootBootstrapState.ts`, `packages/chat/tests/integration/root-page-mounted.test.mjs`, `packages/test/src/chat/scenario-specs/welcome-prompts.spec.ts`, `2026-04-23-post-closure-official-handoff-batch-2.md` |
| K-025 | `candidate` | once a legacy scene has handed off some boundaries, delete those branches immediately instead of keeping the whole scene/spec intact until every boundary is resolved | the first cleanup deletion became straightforward once `edge-overrides` stopped carrying already-handed-off `maxLength`, placement, and header/footer slot branches, leaving only the unresolved compatibility assertions behind | `packages/test/src/chat/edge-overrides.spec.ts`, `packages/test/src/chat/scenarios/BlackboxEdgeScene.vue`, `2026-04-23-post-closure-edge-overrides-pruning.md` |
| K-026 | `candidate` | once the official blackbox entry ladder is frozen, collapse compatibility promotion back to explicit fallback instead of carrying historical owner-aligned legacy subsets as a second supported entry contract | post-closure cleanup got simpler once `resolveRootPageBlackboxConfig` stopped promoting old `ChatConfig` request/display/layout/shell subsets and the tests/docs were allowed to prove only target `TrChatConfig` plus explicit scaffold fallback | `packages/chat/src/runtime/config/blackboxEntry.ts`, `packages/chat/tests/runtime/blackbox-entry.test.mjs`, `packages/chat/tests/integration/trchat-blackbox-root-page.test.mjs`, `2026-04-23-post-closure-blackbox-contract-tightening.md` |
| K-027 | `candidate` | once `page inputs` become the frozen owner boundary, do not keep treating them like compatibility relay; let official-path primitives read them directly | this cleanup slice only became coherent once `ChatHeader`, `ChatMessageList`, `ChatWelcome`, `ChatHistory`, `ChatLayout`, and `ModelSelector` stopped gating page-input fallback behind scaffold-era compatibility semantics, which also let mounted `Root + primitives` proof exercise owner defaults without explicit prop handoff | `packages/chat/src/components/core/ChatHeader.vue`, `packages/chat/src/components/core/ChatMessageList.vue`, `packages/chat/src/components/core/ChatWelcome.vue`, `packages/chat/src/components/core/ChatLayout.vue`, `packages/chat/src/components/model-selector/ModelSelector.vue`, `packages/chat/tests/integration/root-page-mounted.test.mjs` |
