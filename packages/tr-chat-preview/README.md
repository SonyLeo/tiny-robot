# TrChat Preview

Minimal consumer project for validating `TrChat`.

## Start

```bash
pnpm install
pnpm -F @opentiny/tr-chat-preview dev
```

## Env

Copy `.env.example` to `.env`, then fill:

```env
VITE_DEEPSEEK_API_KEY=your_api_key
```
